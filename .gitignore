# Build output
build/
*.exe

# Logs
logs/
*.log

# Setup state / artifacts
setup.log
.setup-state.json

# Sign script side-effects (signatures are regenerated on every build)
# Don't ignore the scripts themselves, only the signature blocks
# (handled by Build-Exe.ps1)

# AI assistant local state (Claude Code session data, etc.)
.claude/

# OS
Thumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editors
.vs/
.vscode/
*.suo
*.user
*.swp

# Local config overrides
config/settings.local.json
